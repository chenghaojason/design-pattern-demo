策略模式（Strategy Pattern）：

定义：定义一系列算法，将每一个算法封装起来，并让他们可以相互替换。每一个封装的算法的类都可以称为策略。
    为了保证这些策略的一致性，一般会用一个抽象的策略类来做算法的定义，而具体每种算法则对应一个具体策略。
    策略模式通常把一个系列的算法封装到一个系列的策略类里面，作为一个抽象策略类里面。

三个角色：
    1.环境类 Context
    2.抽象策略类 Strategy
    3.具体策略类 ConcreteStrategy

策略模式可以结合享元模式使用，以减少对象的数量。