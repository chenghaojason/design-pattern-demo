享元模式（Flyweight Pattern）-结构型模式

享元模式适用于在系统中存在大量相同或相似对象时。使用享元模式可以复用这些相同或者相似的对象。
特点：
    1.创建一个享元工厂来负责维护一个享元池，用于存储具有相同内部状态的享元对象。
    2.享元类中封装相同的内容（内部状态），不同的内容通过外部程序设置（外部状态）。

定义：运用共享技术有效地支持大量细粒度对象的复用。享元模式一般结合工厂模式使用。
